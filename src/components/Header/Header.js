import Search from '../Search/Search';
import Select from '../Select/Select';

import './Header.css';

const Header = () => {
  return (
    <header>
      <Search />
      <Select />
    </header>
  );
};

export default Header;
